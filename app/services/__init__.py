"""Service layer: plain-Python business logic (no Flask imports).

Modules added by later plan steps: scryfall, edhrec, cache (02);
deck_store, deck_model, tags (03); importer, exporter, pricing (05).
"""
